<!-- Independent review by Claude (Opus 5.5), read-only, run 20260922-192050-ef4eae31, of the proof built by Grok. Saved verbatim by the workspace manager. -->

# Erdos1007 at `6de7090`: publish after fixes

The target, `DimensionFourExtremal.proof`, is sound and proves the full upstream claim. The three things blocking publication are all metadata and process, not mathematics.

## Findings by severity

**1. Blocks the Palomar submission: the registry file and README say nothing is proved.**
- `formalization.yaml` still says "Proof work has not begun", "Nothing here is proved", Stage 2, and review "in-progress".
- `README.md:13` still says Stage 2.
- The `automation.detail` field credits Claude Opus 5 with the inlining but never mentions that Grok 4.7 wrote the proof. Grok wrote it, the gpt-5.6-sol red team reviewed the statement, and this review adds a third lineage.
- Submitting this would misstate both the project's status and who did the work.

**2. Blocks Stage 4 exit: no evidence that Comparator was run.**
- `comparator.json` is configured correctly: `enable_nanoda: true`, target `Erdos1007.Palomar.target`.
- The commit's list of gates does not include Comparator or `scripts/audit-probes.sh`.
- `../../docs/PLAYBOOK.md` makes a green Comparator run with nanoda the exit condition for Stage 4. I could not run it read-only.

**3. Fix before the blueprint goes public as the writeup: it is stale and in places overstates what Lean holds.** (`blueprint/src/content.tex`)
- **Nodes point at statements, not proofs.** `thm:target` (237), `thm:witness` (252) and both separating nodes (263, 272) name the Prop definitions. `checkdecls` passes because a definition exists, which says nothing about the proof. They should name `DimensionFourExtremal.proof`, `DimensionFourExtremal.witness.proof` and the two `.separating.proof`.
- **Nodes weaker in Lean than in the text:**
  - `lem:min-degree` (137–154): the node claims minimum degree three. The Lean lemma `unitDistance_of_nine_edges_degree_le_two` only gives the placement in ℝ³. The minimum-degree conclusion exists only as the local `hmin` inside the target proof (`InlineErdos1007Proof.lean:178`).
  - `lem:five-vertices` (181–191): the node claims five vertices are impossible. The Lean lemma gives only the isomorphism to K₅−e. The contradiction is inline in the target proof, lines 193–240.
  - `lem:six-vertices` (220–231): the node claims G ≅ K₃,₃. The Lean lemma `nine_edges_six_minDegree_three` gives "K₃,₃ or the complement is C₆". The prism case is excluded inline, lines 241–259.
  - `lem:k33-dim`: the two halves are proved separately. Dimension exactly four is assembled only inside `witness.proof`, lines 131–148.
  - `lem:monotone`: formalized in the representation form, which the node already admits.
- **Missing `\leanok`** on `lem:monotone`, `lem:kn-minus-e`, `lem:k33-dim`, `lem:min-degree`, `lem:five-vertices` and `lem:six-vertices`. Their "not yet a Lean theorem" wording is now half-true: the step is proved, but only as a `have` inside the target proof.
- None of this weakens the target. Either promote the inline steps to named theorems or reword the nodes.

**4. Not blocking: hypotheses carried but not needed, and linters switched off.**
- **The known five-vertex case is confirmed.** `nine_edges_degree_ge_three_iso_deleteEdge` (`FiveVertices.lean:158`) takes `hdeg : ∀ v, 3 ≤ G.degree v`. I kernel-checked that nine edges on `Fin 5` already force every degree to be at least three.
- **Unused `[DecidableEq V]`, with the linter silenced to hide it:**
  - `nine_edges_card_ge_five` and `nine_edges_minDegree_three_card_le_six` carry it (`NineEdges.lean:72, 87`).
  - The unused-instance linter is switched off for the whole file at `NineEdges.lean:65` and `EightEdges.lean:43`, and per declaration at `LowDegree.lean:45` and `Reattach.lean:222`.
  - `AGENTS.md` says not to weaken linters. The justifying comment, "belongs to the fixed statement", has no basis: nothing fixes these lemma statements. The usual fix is `classical` inside the proof.
- **Lemmas narrower than they could be (fine for the target):**
  - `unitDistance_of_nine_edges_degree_le_two` could take `≤ 9` edges instead of `= 9`.
  - `unitDistance_extend_degree_le_two` is fixed to ℝ³; the argument works in any ℝⁿ with n ≥ 3.
- **These hypotheses really are needed:** `hpos` in LowDegree (K₃,₃ plus an isolated vertex) and `hdeg` in SixVertices (K₅ minus two edges, plus a pendant vertex).

**5. Not blocking: trust.**
- **Axioms:** the target and all three companion proofs depend on `propext`, `Classical.choice` and `Quot.sound` only.
- **No escape hatches:** there is no `native_decide`, `sorry`, `axiom`, `opaque` or `implemented_by`.
- **Raised heartbeats:** `set_option maxHeartbeats 1000000` at `TwoRegularSix.lean:79` is five times the default, for `fin_cases` over `Fin 6`. That costs time, not soundness.
- **`decide`:** every use is on a tiny instance — `Fin` literals, small ℕ comparisons, `choose 4 2`.
- **Classical and instance overrides:** `Classical.decRel` at `InlineErdos1007Proof.lean:168` is harmless, because the statement uses `ncard` and `Set.ncard_eq_toFinset_card'` holds for any `Fintype` instance. The local `attribute [-instance] fintypeEdgeSetSup in` is also harmless.
- **Witnesses are not degenerate:**
  - The unit-distance separator: K₂ plus a vertex, placed at 0, 1, 2 in ℝ¹.
  - The dimension separator: K₂, which has an edge.
  - The non-vacuity witness: K₃,₃ carried onto `Fin 6`, with both bounds on its dimension genuinely proved.
- **Stale build files:** the build outputs for the seven new modules and the proof file were older than their sources. I re-elaborated all of them from source, and all passed clean.

**6. Statement and Compass row 5: I would re-sign.**
- **The statement still means the claim.** Between `d701fd6` and `6de7090`, `UnitDistanceEmbeddable`, `HasDimension` and `DimensionFourExtremal` did not change. Only the two separating definitions and their docstrings did. The substantive changes are in `96c2486`, which predates the provisional sign-off in `61dd603`, plus the new conjunct in this commit.
- **Upstream agreement holds by `rfl`.** The upstream package is not built here, so I copied the upstream definitions verbatim into stdin. Upstream's `ℝ^n` notation is `EuclideanSpace ℝ (Fin n)`, per `FormalConjecturesForMathlib/Geometry/Euclidean.lean:22`. The copied statement is `rfl`-equal to `DimensionFourExtremal`, and the proof typechecks against it.
- **The new conjunct is redundant.** I kernel-checked that the new form is equivalent to the old one: the placement `f` itself witnesses `UnitDistanceEmbeddable G n`. It still separates in the sense row 5 states: some valid placement puts a non-edge at distance one.
- **Optional caveat.** The separation is per placement, not per graph and dimension: for K₂ plus a vertex in ℝ¹, the stricter definition also holds. A per-graph separator would be the six-spoke wheel minus one rim edge, in ℝ², which forces a unit non-edge. That is my own reasoning, not kernel-checked.

## What is worth contributing

**Mathlib:**
- **Three unit spheres meet in at most two points.** `not_forall_dist_eq_one_of_injective_fin_three` is the ℝ³ companion of Mathlib's `EuclideanGeometry.eq_of_dist_eq_of_dist_eq_of_finrank_eq_two` in `Mathlib/Geometry/Euclidean/Basic.lean`.
  - Name: `EuclideanGeometry.eq_of_dist_eq_of_dist_eq_of_dist_eq_of_finrank_eq_three`, or an `encard ≤ 2` form.
  - Changes: an abstract `NormedAddTorsor` with `finrank ℝ V = 3`, any radius, and three centres that are distinct rather than two injective triples. The collinear case gives no common point at all.
- **Raising the ambient dimension.** Contribute the underlying map, not the graph lemma: a linear isometry `EuclideanSpace.extendByZero (e : ι ↪ κ)`, or `(h : n ≤ m)`, into `EuclideanSpace 𝕜 κ`, in `Analysis/InnerProductSpace/PiL2.lean`. Nothing like it exists in the pinned Mathlib. The graph lemma is then a corollary.
- **Two-regular graphs on six vertices: not as is.** It is a case bash that needs raised heartbeats. The Mathlib-worthy statement is that every component of a finite two-regular graph is a cycle.
- **Re-attachment:** needs a unit-distance representation definition, which Mathlib lacks. Upstream `UnitDistanceEmbeddable` and `HasDimension` into `FormalConjecturesForMathlib` first. The reusable geometric pieces are that a sphere, or the circle where two spheres meet, is infinite.

**The formal-conjectures pull request:**
- Add `formal_proof using lean4 at "<public URL at a 40-character SHA>/Erdos1007/Standalone/Mathlib/InlineErdos1007Proof.lean"` to the `@[category research solved, AMS 5 52]` attribute of `erdos_1007.variants.dimension_four_extremal`. This mirrors how `erdos_1007` already links to plby/lean-proofs.
- It needs a public repository first; the work is not pushed.
- The pull request is an outward action, so the owner sends it.

## Findings (ADR 0006)

- **Prior art to cite (unverified).** According to a web-fetch summary, plby's `Erdos1007.lean` proves `dim_K33_eq_4` and `edges_lt_9_embeds_in_3`, has no `sorry`, and does not prove the extremal variant. That overlaps our witness and `EightEdges`. The variant is still unlinked, so the pull request does add something, but the writeup should cite plby. I did not open the file in Lean.
- **Gap in Chaffee–Noble (verified by this Lean proof).** Theorem 7 never rules out seven or more vertices; the blueprint already records this. The paper's claim that the degree sequence (4,4,4,3,3) determines K₅−e is stated without argument and is now proved.
- **Stronger variant (verified in Lean).** On five vertices the minimum-degree hypothesis is unnecessary. This is internal to the project, not an upstream error.
- **Misformalization:** none found upstream. The `rfl` check holds.

**Verdict: publish after fixes (items 1–3).**

## Receipt

Changed:
/Users/dishant/.claude/plans/erdos1007-independent-swirling-dijkstra.md — plan file holding the verdict summary (the only file plan mode allows writing); no repository file changed

Verified: model reported by the harness: Opus 5.5 (claude-opus-5-5).
- `git diff d701fd6 6de7090 -- Erdos1007/Standalone/Mathlib/InlineErdos1007.lean` and `git show 6de7090`: only the separating definitions changed.
- `lake env lean --stdin` with the upstream definitions copied in: `example : Up.Stmt = DimensionFourExtremal := rfl` passed; `DimensionFourExtremal.proof` typechecks as `Up.Stmt`; the equivalence of the new and old separator passed. `#print axioms` on the target, the witness and both separators showed [propext, Classical.choice, Quot.sound].
- `lake env lean --stdin` on the proof file's source, and on each of FinLe, Reattach, EightEdges, LowDegree, NineEdges, FiveVertices and SixVertices: no errors. The target's axioms are the same three.
- `lake env lean --stdin` on the five-vertex degree derivation: passed.
- grep for `set_option`, `decide`, `Classical` and instance overrides: results as in finding 5.
- WebFetch of the plby file: model-summarized, not checked in Lean.

Assumptions: The brief asked for the red team's `rfl` check to be redone only without writing files. I did it by copying the upstream definitions verbatim into stdin, because the upstream package is not built here. I read "the one statement-file change" as the conjunct added in `6de7090`, and reviewed the earlier separator edits as part of the freeze diff. I treated fetching the plby file as a permitted read-only lookup.

Left undone: I did not run Comparator, `lake build`, `lake exe axioms` or `audit-probes.sh`, because the brief forbids building. Someone needs to run `lake build`, then the Comparator run from `comparator.json`, from a clean tree. I did not fully verify the plby overlap. I could not call ExitPlanMode because it is not in my tool set.
